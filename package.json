{
  "name": "mama-informada",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run generate-sitemap && nuxt build",
    "dev": "nuxt dev",
    "generate": "npm run generate-sitemap && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "fetch-blogs": "node scripts/fetch-blogs.js",
    "generate-sitemap": "node scripts/generate-sitemap.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "nuxt": "^4.1.3",
    "pdfmake": "^0.3.11",
    "vue": "^3.5.22",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "contentful": "^11.8.5",
    "vuetify-nuxt-module": "^0.19.5"
  }
}
